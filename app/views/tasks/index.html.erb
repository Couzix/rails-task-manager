<h1>My Tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <li><input type="checkbox" name= "active[]" value=<%= task.completed ? "t" : "f" %> <%= 'checked' if task.completed %>><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
<%= link_to 'Add a new task', tasks_new_path %>
